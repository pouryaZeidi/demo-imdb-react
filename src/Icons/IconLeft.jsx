import React from 'react'
import { CiSquareChevLeft } from "react-icons/ci";

const IconLeft = ({size,color}) => {
  return (
    <CiSquareChevLeft size={size} color={color} />
  )
}

export default IconLeft